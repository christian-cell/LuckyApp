const express = require('express');

const Salud = require('../../models/mascotas/Salud');

const saludRouter = express.Router();

//AQUI DEFINIREMOS LAS PETICIONES CRUD

module.exports = saludRouter;
