const express = require('express');

const Adopcion = require('../../models/mascotas/Adopcion');

const adopcionRouter = express.Router();

//AQUI DEFINIREMOS LAS PETICIONES CRUD


module.exports = adopcionRouter;